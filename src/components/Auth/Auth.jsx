import React from "react";

const Auth = (props) => {
  return (
    <div>
        <h1>Auth</h1>
        <AuthLogin/>
    </div>
  );
};

const AuthLogin = (props) => {
    return (
        <div></div>
    );
  };
  

export default Auth;
